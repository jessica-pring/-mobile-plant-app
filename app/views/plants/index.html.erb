<div class="container">
  <h2 class="text-center mt-2">All plants</h2>

  <div class="card-container">
    <div class="row">
      <% @plants.each do |plant| %>
        <div class="col-xs-4 col-sm-6 col-md-6 col-lg-4 my-2 p-3" id="card">
          <%= link_to plant_path(plant) do %>
            <img src="<%= plant.image_url %>" alt="<%= plant.name %>" style="width: 100%; height: 250px; object-fit: cover">

            <div class="card-info">
              <h4><%= plant.name %></h4>
              <h6><%= plant.latin %></h6>
              <p><%= plant.description.truncate(100, separator: /\s/) %></p>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>
